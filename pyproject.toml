[project]
name = "starwrite"
version = "0.1.0"
description = "Pyxel star sky prototype"
requires-python = ">=3.11"
dependencies = [
    "pyxel>=2.7",
]

[project.optional-dependencies]
dev = [
    "pytest>=8",
]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]

